import styled from "styled-components";
import tw from "tailwind.macro";

export const StyledButton = styled.button.attrs({ className: `h-10 w-auto bg-orange-500 hover:bg-orange-700 text-white font-bold py-2 px-4 rounded` })``;
